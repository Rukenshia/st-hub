image: alpine/edge
packages:
  - nodejs
  - npm
  - aws-cli
  - make
sources:
  - https://github.com/Rukenshia/st-hub.git
tasks:
  - setup: |
      cd st-hub/frontend
      npm ci
  - build: |
      cd st-hub/frontend
      make build
  - deploy: |
      cd st-hub/frontend
      make deploy
